name: Docs artifact check

on:
  pull_request:
    paths:
      - "docs/**/*.md"

jobs:
  check-doc-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 不審な文字(キリル文字等)の混入をチェック
        run: ./scripts/check-doc-artifacts.sh
